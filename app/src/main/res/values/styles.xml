<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/coral</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/mustard</item>
    </style>

    <style name="Button">
        <item name="android:textAppearance">@style/TTBody.Medium</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
    </style>

    <style name="Button.Mustard">
        <item name="android:background">@drawable/mustard_rounded_corner</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Button.Coral">
        <item name="android:background">@drawable/coral_rounded_corner</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Button.Coral.Outline">
        <item name="android:background">@drawable/coral_outlined_rounded_corner</item>
        <item name="android:textColor">@color/coral</item>
    </style>


    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/white_top_rounded_corners</item>
        <item name="behavior_peekHeight">600dp</item>
    </style>
</resources>
